# where dependencies are installed
export GOPATH="$HOME/go"
#
# where golang itself is installed
export PATH=$PATH:/usr/local/go/bin

# where go binaries are installed (go install ...)
export PATH="$PATH:$GOPATH/bin"

